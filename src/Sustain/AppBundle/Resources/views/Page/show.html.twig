{% extends 'AppBundle::layout.html.twig' %}

{% block body -%}
    <div class="col-sm-10 ">
        {% if is_granted('ROLE_ADMIN') %}<a class="btn btn-primary pull-right" href="{{ path('page_edit', { 'id': entity.id }) }}">Edit</a>{% endif %}
        {{ entity.pageBody|raw }}
    </div>


{% endblock %}
