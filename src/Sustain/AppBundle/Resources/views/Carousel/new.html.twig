{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Carousel creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('carousel') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
