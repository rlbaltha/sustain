{% extends 'AppBundle::layout.html.twig' %}

{% block body -%}
    <h1>Labels</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Color</th>
                <th>Sortorder</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tag_show', { 'id': entity.name }) }}">show</a></td>
                <td>{{ entity.color }}</td>
                <td>{{ entity.sortorder }}</td>
                <td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tag_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
