{% extends 'AppBundle::layout.html.twig' %}

{% block body -%}
    <h1>Getting Started</h1>

    {% for entity in entities %}
        <div class="col-sm-2">
            {{ entity.image }}
            {{ entity.description }}
            {{ entity.name }}
        </div>
    {% endfor %}

{% endblock %}
